program addNumbers

! This simple program adds two numbers
   impliciT none

! Type declarations
   integer :: a, b, result 
   character(len=10) :: f
   logical :: name
   a=2
   b=3
   name= .truE.
   print *, "hiii"
   if (.not.(.not. name)) then
   print *, a
   end if

end program addNumbers